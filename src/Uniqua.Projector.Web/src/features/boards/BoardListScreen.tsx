/**
 * SCR-02 — the board list, at `/`. A placeholder the route table points at, so T19 fills it in
 * without editing `app/routes.tsx`.
 */
export function BoardListScreen() {
  return (
    <p className="text-muted-foreground text-sm">Your boards arrive in a later step.</p>
  )
}
