{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Required outside Development: the address of the TLS-ending reverse proxy (sad §7) whose
  // X-Forwarded-For / X-Forwarded-Proto are believed. Empty, the application refuses to start.
  "TrustedProxies": []
}
