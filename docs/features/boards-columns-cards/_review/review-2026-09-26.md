---
feature: boards-columns-cards
date: 2026-09-26
reviewer: sdd:reviewer ×4 (clean context, read-only, one per dimension)
verdict: CHANGES REQUESTED
---

# Review — boards-columns-cards — 2026-09-26

## Scope

- Diff: `91bd75c..HEAD` on `feature/boards-columns-cards` — 47 commits, 164 files, +26,906 / −71.
- Plus one uncommitted working-tree change: `src/Uniqua.Projector.Web/src/features/auth/AccountShell.tsx` (removes `max-w-5xl`).
- Baseline read by the reviewers: `spec.md` §4 (US-01..US-10) and §5 (AC-01..AC-28), `sad.md` §6/§8/§10/§11, `ux-flows.md`, `screens.md`, `data-model.md`, `migrations/`, `contracts/openapi.yaml`, `contracts/api-sync-report.md`, `adr/0012`–`0018`, `docs/adr/0001`–`0005`, `test-plan.md`, `tasks.json`, `CLAUDE.md`.
- Dimensions (one reviewer each): (1) stage-1 end-to-end US + AC trace across both `target_surfaces`; (2) stage-1 backend contract fidelity; (3) stage-2 backend correctness, security, concurrency, tests; (4) web-frontend — UI ACs, `screens.md` conformance, styling, state, licences.
- Checks run by reviewers: `dotnet build` (0 warnings), `dotnet ef migrations has-pending-model-changes` (none), `npm run lint` and `npm run build` (pass), `npm test` (202/203 — `CreateBoardDialog.test.tsx:172` timed out at 5 s in the full run, passes alone).

## What holds

- **Use-case level:** every user story has ≥1 AC, a `sad.md` §6 flow (`sad.md:819-830`) and a `ux-flows.md` flow; every SCR-01..08 is claimed by a `ui` task (T18–T22).
- **Schema:** EF configurations and `20260925154828_CreateBoards.cs` match `data-model.md` and the staged SQL (types, lengths, FKs/cascades, the six indexes + filtered owner index, both rowversions, the version tokens).
- **Contract surface:** all 12 operations mapped with the right method/path/success status; all 20 `boards.*` problem rows match status/type/title/detail; `Retry-After` and `current_*` extensions present; `api/boards.ts` matches the contract.
- **Architecture rules:** reference direction holds, Domain references nothing, invariants live in `Board`/`Card`/`OwnedBoardCounter`, ids from `Ids.New()`, `AppDbContext` stays in Infrastructure, per-layer wiring, every refusal through `ProblemDetailsSetup`.
- **Boundary:** identity from claims only; every read and change loads the board scoped to the caller first; the rate limiter is per account, runs before membership, is bounded and does not count its own refusals; each retry opens a fresh scope and re-decides.
- **Client:** server state only in TanStack Query; `draftStore` holds only typed text (account-tagged, guarded `sessionStorage`); no `dangerouslySetInnerHTML`; Tailwind only; keyboard path for column reordering; every dialog titled; new packages (`react-router`, `@radix-ui/react-dialog` + 23 transitive) all MIT.

## Findings

### Stage 1 — blocks ship

| # | Finding | Cite | AC / contract | Verdict |
|---|---|---|---|---|
| B1 | A session ending discards typed text for four of the six text-carrying changes. The card edit dialog and the board deletion dialog never call `keep()`. The add-column and rename-column drafts are kept, but `offeredItems` lists only `add_card` and `rename_board`, so they are never offered back. No 401 test exists beyond add card. | `BoardScreen.tsx:231-234,252`; `CardDetailDialog.tsx:98-107`; `DeleteBoardDialog.tsx:50-73`; `BoardScreen.test.tsx:559`; screens.md:52,345,432 | AC-28 | **Fix now** |
| B2 | Renaming a column that someone else has deleted drops the typed name. `goneItemOf('rename_column')` is undefined, so no «That column no longer exists.» notice appears, and the re-read removes the editor. The component test only checks that `onRefused` fired. | `BoardScreen.tsx:95-99,236-238,292-297`; `ColumnHeader.tsx:68-72`; `ColumnManagement.test.tsx:339`; screens.md:225 | AC-18b | **Fix now** |
| B3 | The two e2e-through-UI flows in the test plan (thin path; visitor board link + return address) were never built. No browser driver exists and no task claims them. | test-plan.md:20,34,121; `package.json` | AC-01, AC-12, AC-27 | **Fix now** — Playwright (Apache-2.0), serving the built client from `wwwroot` against the SQL Server container |
| B4 | CI runs no client component tests: only lint and build run, not `npm test`. The Quality suites have no named job of their own. `CreateBoardDialog.test.tsx:172` times out in the full run. | `.github/workflows/ci.yml:21-33`; test-plan.md:167,169 | every UI-facing AC | **Fix now** |
| B5 | The AC-17 test for "a refused attempt is not counted" cannot fail: with the clock frozen, counted and refused attempts expire together. Also, AC-17's wording still counts two refusals the design exempts (a non-JSON body and an antiforgery refusal). | `BoardChangeRateLimitTests.cs:124-141`; spec.md:215; sad.md §8/§11; `BoardChangeRateLimit.cs:20-23` | AC-17 | **Fix now** — stagger the clock (+30 s, ≥120 refusals, +31 s, admitted); amend AC-17 to name both exemptions |
| B6a | Unknown request fields are accepted, although every request schema says `additionalProperties: false` and OQ-API-3 answers them with `boards.request_invalid`. | `BoardRequestBody.cs:21-47`; openapi.yaml:1459-1576; api-sync-report.md:322-325 | contract | **Fix now** (code) — reject unknown members in the post-membership shape branch, and add a test |
| B6b | Shape vs owner order on renameBoard/deleteBoard: the two path descriptions say "after the owner check", but `info.description`, the sync report and the code check shape first. `BoardEndpointTests.cs:218-231` pins the code's order. | `BoardEndpoints.cs:170-174,210-214,246-253`; openapi.yaml:38-41 vs 272-275,338-339; sad.md §6 | AC-22 / contract | **Fix now** (contract) — correct the two path descriptions and sad.md §6 to the global order |
| B6c | A non-UUID column/card path id short-circuits to `not_available` before the shape check, while a well-formed missing id gets `request_invalid`. This does not leak membership. | `BoardEndpoints.Columns.cs:86,125,164`; `BoardEndpoints.Cards.cs:89,121,164`; openapi.yaml:982-999 | contract | **Fix now** (code) — map an unparsable column/card id to `Guid.Empty`, as `Cards.cs:62` already does |
| B6d | The board-list cursor holds only an id, looked up by position, so a cursor naming a deleted board returns 400. The contract says it encodes `(created_at, id)`. | `BoardEndpoints.cs:326-344`; openapi.yaml:1340-1342 | contract | **Fix now** (contract) — describe the cursor as it works today; the key-based seek belongs to roadmap step 7 |
| B6e | The retry bound is stated three ways: the XML doc says "at most 3 retries", the code does 3 attempts, and ADR 0015 line 29 disagrees with its line 45. | `BoardChangeRetry.cs:34-35`; adr/0015:29,45 | ADR 0015 | **Fix now** (wording) — "3 attempts (2 retries)" in the comment and ADR line 29; keep `MaxAttempts = 3` |
| B7 | Test-plan and test gaps: (a) test-plan.md:99 contradicts screens.md:425 on whether Delete is disabled; (b) test-plan.md:131 contradicts ADR 0014 on a malformed body from a non-member; (c) AC-17 and AC-21 have no `ui` task, and the member-default test never asserts that the column/card controls are present (`BoardScreen.test.tsx:216-237`); (d) integration rows prove less than claimed — AC-02 (over-100, invalid rename, nothing written), AC-14 (invalid edit), AC-18/18b (edit a deleted card, delete a deleted column, add to a deleted column, full-field comparison against a random id), AC-19 (other members' lists), AC-20 (probe former columns/cards), AC-17 (non-member board in the identical-refusal set), AC-21 (member-run refusals), AC-25 (no board content in logs), AC-28 (session expiry via the clock), AC-06b/AC-23 (a genuine second session). | test-plan.md:88,99,103,131; `BoardEndpointTests.cs:46-56,129-146`; `CardEndpointTests.cs:231,305-317`; `ColumnUseCaseTests.cs:296-315`; `BoardUseCaseTests.cs:203-219`; `BoardChangeRateLimitTests.cs:65-106`; `BoardBoundaryTests.cs:219-281` | AC-02, 06b, 14, 17, 18, 18b, 19, 20, 21, 23, 25, 28 | **Fix now** — correct (a) and (b); add the missing cases and assertions |
| B8 | Spec §8 questions 2 (D5 ADR) and 4 (smoke workload) are still unticked, though ADR 0013 and sad.md §10 closed them. The security review §6.1 requires is not recorded. | spec.md:338,349,351 | n/a | **Fix now:** tick questions 2 and 4 with references. **Defer:** the security review → spec §8 (owner: Security Lead, due: before `/sdd:ship boards-columns-cards`) |

### Stage 2 — quality

| # | Finding | Cite | Verdict |
|---|---|---|---|
| Q1 | A lone-surrogate escape (`{"name":"\ud800"}`) makes `GetString()` throw, and every change endpoint answers 500 after using a rate-limit slot. Reproduced on the repo's SDK. | `BoardRequestBody.cs:27`; `BoardEndpoints.Cards.cs:235` | **Fix now** — treat it as not-a-string → `request_invalid`; add an endpoint test |
| Q2a | The race suite for simultaneous changes is vacuous for 4 of 5 pair kinds. `ContentionForcer` bumps on every attempt of both racers, so both always end `contended`, `successes` is always 0, and the ceiling assertions cannot fail. The natural-race use-case tests do not guarantee overlap either. | `BoardInvariantRaceTests.cs:82-86,114-131,157-174,193-210,278-295`; `ContentionForcer.cs:116-128`; `ColumnUseCaseTests.cs:74,218`; `CardUseCaseTests.cs:58`; `BoardUseCaseTests.cs:88` | **Fix now** — give the forcer a one-bump budget; assert exactly one success plus the loser's own refusal; treat `contended` as a failure |
| Q2b | The add-card-to-X / delete-X race is never run. If the insert commits first, the batched DELETE hits FK 547, which `SaveAsync` does not translate, so the member gets a 500 instead of `column_not_empty` after a retry. The invariant itself holds via the FK. | `BoardInvariantRaceTests.cs:222-238`; `BoardStore.cs:96` | **Fix now** — map `SqlException.Number == 547` to `BoardConcurrencyConflict`; add the pair |
| Q3 | The latency baseline is written before the assertions as max/min of previous and current, so it only loosens and a failing run lowers its own bar. It is written into the source tree. The "under 120/minute" pacing comment is wrong under the frozen clock. | `BoardLatencyBudgetTests.cs:84-90,407-414,447-448` | **Fix now** — write only on a pass, compare with the last recorded run, keep it as a deliberately committed artefact, advance the clock with the workload |
| Q4a | The add-card form shows the previous refusal after Cancel and reopen, and its button does not read «Adding…» while pending. | `AddCardForm.tsx:61,81-85,124-134`; screens.md SCR-04 | **Fix now** |
| Q4b | The route-guard test uses `toHaveTextContent('/')`, which matches `//evil.example` itself. The control-character and percent-encoded cases are missing. The guard itself is sound. | `routes.test.tsx:22-32,164`; `routes.tsx:20-30` | **Fix now** |
| Q4c | The create-board "server refusal" test is refused client-side and never reaches the mocked 400. It is also the slow test that timed out. | `CreateBoardDialog.test.tsx:172-186` | **Fix now** |
| Q4d | An empty-body deleteBoard is refused before membership as `request_malformed`, while deleteColumn/deleteCard answer `request_invalid` after membership. | `BoardEndpoints.cs:193` vs `BoardEndpoints.Columns.cs:155`, `BoardEndpoints.Cards.cs:155` | **Fix now** — bind `JsonElement?` |
| Q4e | `BoardError.Detail` carries board, column and card names. It is unused today, but one log call would leak them. | `BoardError.cs:53-55,71-73,89-92,118-120`; sad.md §8 Logging | **Fix now** — fixed sentences; typed properties for current state |
| Q4f | Refusal paths call `OpenBoard` and read up to 1,000 card summaries they do not need. | `BoardEndpoints.cs:231,249`; `BoardEndpoints.Columns.cs:226`; `OpenBoard.cs:25` | **Fix now** |
| Q4g | The rename endpoint re-applies `BoardText.Trim` instead of returning what the domain stored. | `BoardEndpoints.cs:184-185` | **Fix now** |
| Q4h | A duplicate "non-UUID" boundary scenario sends the same valid-UUID GET. | `BoardBoundaryTests.cs:72-73` | **Fix now** |
| Q4i | The seven new UI components are not registered; `docs/design-system.md` does not exist. | screens.md:17,491-499 | **Defer** → spec §8 (owner: Alex Korneiko, due: before `/sdd:screens` of the next UI feature, via `/sdd:design-system`) |
| Q5 | The uncommitted removal of `max-w-5xl` also leaves SCR-02 and SCR-08 unbounded. `mx-auto` becomes dead code. | `AccountShell.tsx:95-96` (working tree) | **Fix now** — keep the shell full-width; `max-w-3xl` on `BoardListScreen` and `BoardNotAvailable`; drop `mx-auto`; commit with the reason |
| Q6 | The race suite accepts ≥1 forced collision out of 1,000 pairs; `BoardLatencyBudgetTests` times from the client, while §6 says server-side. | `BoardInvariantRaceTests.cs:82-86`; `BoardLatencyBudgetTests.cs:84-90` | Folded into Q2a / Q3 |

No finding was dismissed as not-an-issue.

## Gate

**CHANGES REQUESTED.** All stage-1 findings except the security-review record are **Fix now**, and that record is deferred with an owner and due date in spec §8. Once the fixes land, re-review the changed surface (`BoardScreen`, `CardDetailDialog`, `DeleteBoardDialog`, `BoardRequestBody`, `BoardEndpoints*`, `BoardStore`, the Quality suites, CI, `openapi.yaml`, `test-plan.md`, `spec.md`), then ship.
