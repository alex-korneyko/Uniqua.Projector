# Review — accounts-and-sessions — 2026-09-23 (fourth re-review)

**Gate result: CHANGES REQUESTED**

## Scope

- This is the fourth re-review. The third re-review (`review-2026-09-23-2.md`, committed at effda2c) asked for changes. Its fix-now findings were built as T54–T61 in the range `effda2c..HEAD` (HEAD 7921fb7). That range changes 32 files: 2,048 lines inserted and 98 deleted.
- The end-to-end trace was rebuilt over the whole feature, `9736557..HEAD` (the merge base with `main`). It covers both declared surfaces, `backend-service` and `web-frontend`.
- The baseline was:
  - spec §4 and §5: 7 user stories and 18 ACs;
  - sad §6 and §7, data-model.md and contracts/openapi.yaml;
  - ADRs 0006–0011 and test-plan.md;
  - tasks.json, T1–T61.
- Three independent, clean-context `sdd:reviewer` passes ran:
  1. a finding-by-finding check of S-01 to S-05 and T-01 to T-06 against the current code and tests;
  2. a stage 1 end-to-end trace of every user story and AC, plus contract fidelity;
  3. a stage 2 quality pass over the fix range: backend, tests and `.gitignore`.
- The gate was run separately, all green:

  | Check | Result |
  |---|---|
  | `dotnet build` | 0 warnings, 0 errors |
  | Domain tests | 63/63 |
  | Integration tests (SQL Server container) | 305 passed, 1 skipped, 0 failed |
  | `dotnet format --verify-no-changes` | exit 0 |
  | `oxlint` | exit 0 |
  | Web build (`tsc -b` + Vite) | passes |
  | Vitest | 61/61 |

  The skipped test is the redeploy scenario, still deferred to roadmap step 4.
- The author of this record checked the following directly against the code:
  - V-01: `AccountEndpoints.cs:98-99` measures the untrimmed `request.Email.Length`, and `:209,212` repeat `Account.MaxEmailLength` and `Account.MaxPasswordLength`.
  - V-04: `RequestShapeTests.cs:414` calls `UseHttps()`, the comment at `:385-387` says plain HTTP, and no workflow under `.github/workflows` runs `dev-certs`.
  - U-02: `openapi.yaml:558` declares `additionalProperties: false`, and nothing under `src` sets `JsonUnmappedMemberHandling`.
  - U-03: `openapi.yaml:387-395` still carries `# unresolved — OQ-API-1`.

## Prior findings (S-01 to S-05, T-01 to T-06)

- **Resolved (9):** S-01, S-02, S-04, S-05 and T-01–T-04, T-06. Each has a test that fails against effda2c. The S-02 tests fail mainly because `TrackedAddressKeyCount` did not exist, since the behaviour they pin was already there.
- **Partial (2):**
  - S-03: the three requested places were reworded, but six others still state the old rule (U-01).
  - T-05: the cited patterns were anchored, but `publish/`, `x64/`, `x86/` and the ARM patterns were not (V-07).
- Concurrency in `SlidingWindowLimiter`, checked by two passes:
  - Release against Reserve loses nothing, because of the `ReferenceEquals` re-check under the list lock.
  - The `Interlocked` tracked-key count is exact and cannot go below zero.
  - The compare-and-set throttles let only one thread prune or log.
  - No deadlock is possible.
- Still deferred in spec §8 with owner and due date: R-12, R-24, R-27, and AC-09 (to roadmap step 8).

## Trace summary

- All seven user stories have at least one AC and a sad §6 flow. US-07 is covered as non-runtime.
- 15 ACs are complete from spec through to a test: AC-01b, AC-02, AC-02b, AC-03, AC-05, AC-05b, AC-06, AC-07, AC-07b, AC-08, AC-10, AC-11, AC-11b, AC-12 and AC-13. The UI ACs reach a component test under `src/Uniqua.Projector.Web`.
- AC-01 and AC-04 are complete except for the "shown their own display name" UI half, which stays deferred (R-12).
- AC-09 stays deferred to roadmap step 8.
- AC-12's reworded reset clause has no test (U-06).
- All 14 problem codes agree across openapi.yaml, `AccountProblems.cs` and `AccountProblemsTests.cs`, including the `api.*` type URIs: 11 `accounts.*` and 3 `api.*` (`unexpected`, `request_too_large`, `request_rejected`). The migrations did not change in the fix range.
- **Open gaps:**
  - The docs don't all state the reset rule the same way (U-01).
  - Three contract clauses don't match the code (U-02, U-03, U-05).
  - The new over-long-credential branch is missing from sad flow 4 and from test-plan (U-04).

## Findings

The owner resolved every finding below as **Fix now**. They go back to `/sdd:implement` as follow-up tasks.

### Stage 1

| # | Finding | Cite | AC | Verdict |
|---|---|---|---|---|
| U-01 | The reworded reset rule, "15 minutes in which no failed attempt reached verification", reached spec, session-rules.md and test-plan only. Six other places still say "no attempt at all" or "idle". This includes the decision-driver and consequence bullets of ADR 0010, although its own amendment says the ADR "now reads" the new wording. | `sad.md:446,578`; `adr/0010…md:23,45`; `README.md:18`; `data-model.md:132`; `Domain/Accounts/GuessingDelay.cs:23,41` | AC-12 | Fix now: reword all six. Extend `SessionRulesDocumentTests` to check README.md as well. |
| U-02 | openapi declares `additionalProperties: false` on both request bodies, but the records accept unknown fields. `{"email":…,"password":…,"x":1}` gets a 201 today, no test covers it, and the difference is recorded nowhere. `tasks/13-…md:151` says such a body is "Refused". | `openapi.yaml:558,586`; `Api/Accounts/AccountEndpoints.cs:204-222` | contract | Fix now, in code: add `[JsonUnmappedMemberHandling(JsonUnmappedMemberHandling.Disallow)]` to both request records, so an unknown field lands on the existing 400 `accounts.request_malformed`. On sign-in the refusal depends only on the body's shape, so it adds no AC-05b oracle. Add one test per endpoint. |
| U-03 | The code already hands out the token on every safe response, as a readable `XSRF-TOKEN` cookie. openapi still marks acquisition "unresolved — OQ-API-1", owned by a pipeline stage rather than a person, and spec §8 has no entry. A client written from the contract alone can't get the token it is told is required. | `openapi.yaml:387-395`; `Api/Antiforgery/AntiforgerySetup.cs:13-29,90-105`; `contracts/api-sync-report.md:188`; `spec.md:236-245` | contract (§6.1 forgery) | Fix now:<br>• declare the `XSRF-TOKEN` Set-Cookie on GET `/api/v1/accounts/me`;<br>• describe the cookie-and-header pair in `AntiforgeryToken`;<br>• drop the unresolved marker;<br>• close OQ-API-1 in api-sync-report.md. |
| U-04 | The over-long-credential refusal S-01 added is declared in openapi and ADR 0010 and is in the code, but not in the flow or the plan. It returns before the rate limit and before any account lookup. sad flow 4 goes straight to "Reserve a slot", and test-plan has no row for it. | `sad.md:324-330`; `test-plan.md:67-86`; `AccountEndpoints.cs:98-103` | AC-05b, AC-12 | Fix now (docs):<br>• add an `alt` branch before `Reserve` in flow 4;<br>• add a test-plan edge case: email of 257 or password of 129 characters → 401 with an identical body, and no slot taken. |
| U-05 | `RequestRejected` gives 411, 414 and 431 as examples, but Kestrel most likely answers those while parsing, with no body, before the app runs. openapi's own text says such rejections have no body of this shape. Only synthetic endpoints test the mapping. GET `/accounts/me` and DELETE `/sessions/current` also don't declare 4XX, although the hook would answer a 405 on them. | `openapi.yaml:453-462`; `Api/ProblemDetailsSetup.cs:81-98`; `RequestShapeTests.cs:149-193` | contract | Fix now (contract):<br>• keep the examples that reach the app, 408 and 405;<br>• say that 411, 414 and 431 come back from Kestrel with no body;<br>• declare 4XX on the two remaining operations. |
| U-06 | AC-12's reworded clause, "a 429 … does not, by itself, keep the count alive", has no test. The existing reset tests cover only a window with no attempts at all. | `spec.md:186`; `Accounts/SignInTests.cs:203`; `Quality/GuessingProtectionTests.cs:53` | AC-12 | Fix now. Add an integration test on `TestClock`:<br>• 20 failures cap the pair;<br>• 429s keep arriving until 15 minutes after the last verified failure;<br>• the next wrong password is verified and counted as failure #1, with no delay. |

### Stage 2

| # | Finding | Cite | Verdict |
|---|---|---|---|
| V-01 | Two reviewers reported this separately. Sign-in measures `request.Email.Length` *before* trimming, while registration, the store lookup and the cap key all trim. An owner registered with a 256-character address who pastes it with a trailing space or newline is refused every time. The limits are also written out again in Api instead of using the Domain constants. That breaks "Domain rules live in Domain": raising the password limit would lock out longer passwords at sign-in. | `Api/Accounts/AccountEndpoints.cs:98-99,209,212`; `Domain/Accounts/Account.cs:16,24,57,90` | Fix now:<br>• compare `request.Email?.Trim().Length` against `Account.MaxEmailLength`, and the password against `Account.MaxPasswordLength`;<br>• delete the Api copies;<br>• test a padded 256-character address. |
| V-02 | Two reviewers reported this separately. Both sign-in limiters share one logger and write the same `tracked_source_ceiling_reached consequence=source_not_rate_limited` line. When the per-address table fills, which happens far sooner (S-02), the per-source cap of 100 still holds, so the line misstates the effect. An operator can't tell which table is full. | `Api/Accounts/SlidingWindowLimiter.cs:241-246`; `SignInRateLimit.cs:78-79` | Fix now:<br>• pass a limiter name and a consequence into `SlidingWindowLimiter`, and log `limiter=registration\|sign_in_per_source\|sign_in_per_address`;<br>• give the per-address limiter its own consequence;<br>• assert the name in `SignInRateLimitCeilingTests`. |
| V-03 | If the host clock steps back by d, for example an NTP correction after a Proxmox VM resumes, `now − last` is negative, and both the forced reclaim and the ceiling alarm treat it as "not due" for d. A limiter at capacity then turns new sources away uncounted, and nothing is logged. Before T55 the reclaim had no throttle, so this could not happen. | `Api/Accounts/SlidingWindowLimiter.cs:192,211,234` | Fix now. Treat a negative elapsed time as due, or stamp the throttles from `TimeProvider.GetTimestamp()`. Add a `TestClock` test that steps the clock back. |
| V-04 | Two reviewers reported this separately. The real-socket 413 test calls `UseHttps()` with no certificate, so it needs the ASP.NET Core dev certificate. `ci.yml` never creates one, and the class comment says "Plain HTTP, not HTTPS". | `tests/…/RequestShapeTests.cs:251-253,385-387,414`; `.github/workflows/ci.yml` | Fix now. Build a throwaway self-signed certificate in the test with `CertificateRequest`, which is in the BCL and adds no dependency, and pass it to `UseHttps`. Fix the comment. |
| V-05 | Nothing tests that a per-address refusal gives back its per-source slot. Deleting `_perSource.Release(...)` leaves every test green. Without it, about 80 more retries at a capped pair would use up the source's ceiling and block correct passwords for every other address. That is the P-01 regression. | `Api/Accounts/SignInRateLimit.cs:130-133`; `Accounts/SessionEndpointTests.cs:354-380` | Fix now. Cap one pair at 20, send about 100 more refused attempts at it, then assert that `Reserve(source, otherAddress)` is permitted. |
| V-06 | Release against Reserve on one key is correct only because of the `ReferenceEquals` re-check. Nothing would catch its removal, because `Racing_first_reserves_…` races only `TryAdd`. | `Api/Accounts/SlidingWindowLimiter.cs:131,176-180` | Fix now. Add a multi-thread Reserve/Release test on one key. At the end, assert that the outstanding reservations still trigger a refusal at `PermittedPerWindow`. |
| V-07 | T-05 leftover: `publish/`, `x64/`, `x86/`, `[Aa][Rr][Mm]/` and `[Aa][Rr][Mm]64/` are still unanchored. For example, `src/Uniqua.Projector.Web/src/features/publish/x.ts` would be ignored today. | `.gitignore:24-27,31` | Fix now. Anchor them, or drop them where `bin/` already covers the output. |

Clean on this pass:
- no dependency changes, so licensing is unchanged;
- layering, DI and `Program.cs` wiring are unchanged;
- the log lines carry no emails or passwords, and the per-address key is the source plus the SHA-256 of the address;
- every 4xx and 500 goes through the one ProblemDetails handler with a fixed detail, and Development and Production give the same code and status;
- the over-long refusal matches a wrong password in body, headers and slot use, and depends on length only;
- no existing test was weakened;
- `git ls-files -ci --exclude-standard` lists nothing.

## Gate

**CHANGES REQUESTED.** Six stage-1 findings (U-01–U-06) and seven stage-2 findings (V-01–V-07) are Fix now. The stage-1 gaps are in the docs, the contract and one missing test. No AC is unmet in behaviour. Once the fixes are in, re-review the changed surface:
- the sign-in length guard and its use of the Domain constants;
- the request records' unknown-member handling;
- the `SlidingWindowLimiter` limiter name, consequence and clock-step handling;
- the new limiter tests and the AC-12 reset test;
- the Kestrel test certificate;
- `.gitignore`;
- openapi (antiforgery, `RequestRejected`, 4XX);
- sad flows 4 and 6, sad §10 QG-1, test-plan, ADR 0010, data-model.md, README and `GuessingDelay.cs` comments.
