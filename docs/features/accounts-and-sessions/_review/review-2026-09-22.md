# Review — accounts-and-sessions — 2026-09-22

**Gate result: CHANGES REQUESTED**

## Scope

- Branch `feature/accounts-and-sessions`. The implementation diff is `0f05e30..HEAD`: 0f05e30 is the scaffold commit, and the range covers everything from 3c56e49 up to the amended HEAD 2159e4b.
- 101 files changed, about 12,600 lines inserted (docs excluded). It covers both declared surfaces, `backend-service` and `web-frontend`.
- The baseline was spec §4/§5 (7 user stories and 18 ACs), sad §6, data-model.md, contracts/openapi.yaml, ADRs 0006–0010, test-plan.md and tasks.json (T1–T20).
- Three independent, clean-context `sdd:reviewer` passes ran, one per dimension:
  1. stage 1, the AC compliance and end-to-end trace;
  2. stage 1 contract fidelity plus stage 2 backend quality;
  3. stage 2 web-frontend quality.
- The reviewer's checks while running:
  - The web suite passes (45/45), `oxlint` is clean and `tsc -b` exits 0.
  - The backend migrations match data-model.md.
  - Project references, ids, `AppDbContext` confinement and ProblemDetails all follow CLAUDE.md.
  - Every NuGet and npm package the diff adds is MIT or Apache-2.0.
- The author of this record verified four findings directly against the code: R-01, R-02, R-06 and R-08.

## Trace summary

- Every §4 user story has at least one AC and a sad §6 flow.
- Every §5 AC is claimed by at least one task. None of them carries an `added-by-fix` marker.
- AC-09 is deferred explicitly: spec §8 and test-plan.md say it waits for roadmap step 8. Its port-level part is implemented and tested.
- Complete: AC-02, AC-02b, AC-05, AC-08, AC-11.
- Gaps, listed below: AC-01, AC-01b, AC-03, AC-04, AC-05b, AC-06, AC-07, AC-07b, AC-10, AC-11b, AC-12, AC-13.

## Findings

Verdicts: **Fix now** means it goes back to `/sdd:implement` as a follow-up task. **Deferred** means it is recorded in spec §8. **Done** means it was resolved during this review.

### Stage 1

| # | Finding | Cite | AC | Verdict |
|---|---|---|---|---|
| R-01 | The progressive delay starts one attempt late. With 5 prior failures, `For(5)` returns zero, and the 10th failure gets 16 s instead of ≥ 30 s. The tests use the same off-by-one, and one of them asserts `>= 0`. | `Domain/Accounts/GuessingDelay.cs:48`, `Application/Accounts/SignIn.cs:45`, `Quality/GuessingProtectionTests.cs:30-32`, `Accounts/SignInTests.cs:151-153` | AC-12 | Fix now |
| R-02 | After 15 quiet minutes the next delay is skipped, but the count is never reset. `RecordFailureAsync` always writes `AccessFailedCount + 1`, so the second typo after a quiet period gets the 5-minute ceiling. Fix: put the reset rule in Domain and apply it inside the single UPDATE. | `Infrastructure/Accounts/IdentityAccountStore.cs:143-154`, `Accounts/SignInTests.cs:186` | AC-12 | Fix now |
| R-03 | The failure is recorded only after the delay, and the delay runs on the request-abort token. A guesser who hangs up skips both the wait and the count, and parallel attempts all read the same old count. Fix: record the failure straight after verification with `CancellationToken.None`, before the delay. | `Application/Accounts/SignIn.cs:56-57` | AC-12 | Fix now |
| R-04 | An unknown address is never delayed. After 5 failures a registered address answers in ≥ 2 s and an unregistered one in about 150 ms, so the timing reveals which addresses are registered. Fix: an in-memory count per unknown address, keyed by a hash of the normalised address, with the same curve. | `Application/Accounts/SignIn.cs:34-41` | AC-05b | Fix now |
| R-05 | The 5-minute ceiling on the delay is recorded only in `docs/session-rules.md:108`. AC-12 says the delay "grows with each additional failure". Record the ceiling in spec §6 or as an amendment to ADR 0010. | `Domain/Accounts/GuessingDelay.cs:32` | AC-12 | Fix now (docs) |
| R-06 | The session cookie has no `Max-Age` or `Expires`, so the session ends when the browser closes. The tests replay the raw cookie, and the openapi examples omit it too. | `Api/Accounts/SessionCookie.cs:69-80`, `contracts/openapi.yaml:82,221` | AC-06 | Fix now |
| R-07 | Expiry is measured from an activity stamp written at most once an hour. A session can therefore end up to 59 minutes *before* 14 days of inactivity, while spec §6 allows up to 1 hour *after*. | `Domain/Accounts/Session.cs:66-67,77` | AC-07 | Fix now |
| R-08 | Every visitor, including one whose session expired, was revoked or signed out, gets "Create an account". The switch to sign-in is below the fold. No ui task claims AC-07 or AC-07b, and `VisitorScreens` has no test. | `Web/src/features/auth/VisitorScreens.tsx:15`, `main.tsx:14-16`, `AccountShell.test.tsx:17-19` | AC-07, AC-07b, AC-10 | Fix now |
| R-09 | A 401 on a `/me` refetch keeps the cached account view, and there is no global 401 handler. The test "a 401 arriving from any call at all" only repeats the sign-out case. | `Web/src/features/auth/useSession.ts:78`, `Web/src/api/queryClient.ts:8-12`, `AccountShell.test.tsx:231-244` | AC-10 | Fix now |
| R-10 | The client shows only the problem's `detail`, which for these codes is the "because" clause. The plain statements the ACs require ("already registered", "taken", "temporarily limited") are only in `title`, which is discarded. The tests lock in the current text. Also: the client overrides the server's `antiforgery_failed` detail, and the retry sentence could be printed twice. | `Web/src/features/auth/accountRefusals.ts:38-53,76-86`, `Web/src/api/accounts.ts:172-183`, `RegisterScreen.test.tsx:189-224` | AC-03, AC-11b, AC-01b | Fix now (client renders `title` + `detail`) |
| R-11 | The registration rate limit counts permitted *attempts*, not accounts registered, so five refused submissions block a valid sixth one. | `Api/Accounts/RegistrationRateLimit.cs:53-56`, `AccountEndpoints.cs:29` | AC-01b | Fix now |
| R-12 | The "then shown their own display name" outcome is never asserted through the rendered UI. The screen tests assert text that the components never render. | `RegisterScreen.test.tsx:281-292`, `SignInScreen.test.tsx:228-238` | AC-01, AC-04 | Deferred (spec §8) |
| R-13 | The trusted-proxy half of the request-source rule has no test. T13's DoD requires it. | `Api/Program.cs:45-58`, `RegisterEndpointTests.cs:275` | AC-01b | Fix now (with R-17) |
| R-14 | Valid addresses such as `o'brien@example.test`, or ones with non-ASCII letters, are refused as "display name taken". This comes from Identity's default `AllowedUserNameCharacters` plus `InvalidUserName` being mapped to `DisplayNameTaken`. Any other unexpected Identity error falls back to `EmailTaken`. | `Infrastructure/Accounts/IdentityAccountStore.cs:99-103,223` | AC-02b, AC-11b | Fix now |
| R-15 | Register and sign-in return the email as typed in that request (untrimmed, in whatever case was used), while `/me` returns the stored one. `accounts.display_name_invalid` is emitted but not declared in openapi.yaml. | `Api/Accounts/AccountEndpoints.cs:59,90`, `Api/AccountProblems.cs:271` | contract | Fix now (+ `/sdd:api --reconcile`) |
| R-16 | The AC-13 row in the test plan (the id stays the same across sign-out, failures and a new sign-in) is not implemented. | `test-plan.md:67`, `RegisterAccountTests.cs:38`, `SessionEndpointTests.cs:30-48` | AC-13 | Fix now |

### Stage 2

| # | Finding | Cite | Verdict |
|---|---|---|---|
| R-17 | `TrustedProxies` is never configured. Behind the reverse proxy in sad §7, `Request.IsHttps` is false, so antiforgery with `SecurePolicy.Always` throws a 500 on every GET, and every visitor shares the proxy's address. Fix: outside Development, refuse to start when it is empty, and document it in appsettings.json. | `Api/Program.cs:40-58,80-91`, `Api/Antiforgery/AntiforgerySetup.cs:48,92` | Fix now |
| R-18 | Commit a3d7523 committed `appsettings.Development.alex-work-book.json` with an `sa` password, and restated `TargetFramework` and `LangVersion 14` against `Directory.Build.props`. | `Api/appsettings.Development.alex-work-book.json:9`, `Api/Uniqua.Projector.Api.csproj:3-8` | **Done**, amended into 2159e4b before push: the file is untracked and git-ignored as `appsettings.*.*.json` (it stays on the developer's machine) and the restated properties are removed. The reviewer's "duplicate `AddEnvironmentVariables`/`AddCommandLine`" point was **Not an issue**: re-adding them after the machine file keeps environment variables and command-line args above it. `AspNetCoreHostingModel` and the `Stage` configuration were kept because they are not restated. |
| R-19 | `index.css` `@theme` does not define `--color-destructive`, `card`, `card-foreground`, `input` or `ring`. As a result, refusals render in body colour and inputs have no themed border or focus ring. | `Web/src/index.css:6-15` | Fix now |
| R-20 | `invalidateQueries` is called with `void`, so the form is re-enabled while `/me` refetches and a second click sends another POST. Fix: `setQueryData` from the 201 body, then invalidate. | `RegisterScreen.tsx:44-48`, `SignInScreen.tsx:44-47` | Fix now |
| R-21 | `maxLength` on the registration password silently cuts off a longer pasted password. | `RegisterScreen.tsx:114` | Fix now |
| R-22 | Sign-out has no pending or disabled state, so a double click sends two DELETEs. | `AccountShell.tsx:61`, `useSession.ts` | Fix now |
| R-23 | `Program.cs` registers inner-layer types directly, against the "Layer wiring" rule in CLAUDE.md. | `Api/Program.cs:26-30,60-72` | Fix now (`AddAccountsApi()`) |
| R-24 | Migrations never run at startup in development. | `Api/Program.cs:76-118` | Deferred (spec §8) |
| R-25 | `RegistrationRateLimit.Prune()` is never called, so the dictionary grows without bound. | `Api/Accounts/RegistrationRateLimit.cs:73` | Fix now |
| R-26 | `HasNotSucceededRecently` is never surfaced, so the "cleanup has not succeeded for more than 48 hours" alert from sad §7 cannot fire. | `Api/Accounts/ExpiredSessionCleanupService.cs:53` | Fix now |
| R-27 | On the registration screen, a field refusal is not linked to its input through `aria-describedby`. | `RegisterScreen.tsx:116,133,143` | Deferred (spec §8) |
| R-28 | Test-only global types (`vitest/globals`, jest-dom) are part of the app's TypeScript program. | `Web/tsconfig.app.json:8`, `vite.config.ts` | Fix now (split `tsconfig.test.json`, drop `globals: true`) |
| R-29 | `lightningcss` (MPL-2.0, weak copyleft) comes in at build time through `@tailwindcss/vite`, and no licence exception for it is recorded. It predates this diff. | `Web/package-lock.json` | Fix now (record the exception; build-time only, per the owner's standing position on MPL-2.0 build tools) |

## Gate

**CHANGES REQUESTED.** Fifteen stage-1 findings are Fix now (R-01–R-11, R-13–R-16), so the feature does not yet meet its spec. R-12 is deferred with an owner and a due date. Twelve stage-2 findings are Fix now, R-24 and R-27 are deferred, and R-18 is done.

After the fixes, re-review the changed surface. The `SignIn` and `GuessingDelay` code and tests, `SessionCookie`, `Session`, the web visitor and session flow, `RegistrationRateLimit`, `IdentityAccountStore`, and `Program.cs` wiring all need a fresh look.
