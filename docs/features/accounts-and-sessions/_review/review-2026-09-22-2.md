# Review — accounts-and-sessions — 2026-09-22 (re-review)

**Gate result: CHANGES REQUESTED**

## Scope

- This is a re-review. The first review of the day (`review-2026-09-22.md`, committed at 2ec5604) asked for changes. Its fix-now findings were built as T21–T34 in the range `2ec5604..HEAD` (HEAD 8e208b3). That range changes 75 files: 2,592 lines inserted and 323 deleted.
- The end-to-end trace was rebuilt over the whole feature, `0f05e30..HEAD`: 115 files, about 14,000 lines inserted, docs excluded. It covers both declared surfaces, `backend-service` and `web-frontend`.
- The baseline was:
  - spec §4 and §5: 7 user stories and 18 ACs;
  - sad §6, data-model.md and contracts/openapi.yaml;
  - ADRs 0006–0011 and test-plan.md;
  - tasks.json, T1–T34.
- Three independent, clean-context `sdd:reviewer` passes ran:
  1. a finding-by-finding check of R-01 to R-29 against the current code and tests;
  2. a stage 1 end-to-end trace of every user story and AC, plus contract fidelity;
  3. a stage 2 quality pass over the fix range, backend and web.
- The gate was run separately, all green:

  | Check | Result |
  |---|---|
  | `dotnet build` | 0 warnings, 0 errors |
  | Domain tests | 63/63 |
  | Integration tests (SQL Server container) | 232 passed, 1 skipped |
  | `dotnet format --verify-no-changes` | exit 0 |
  | `oxlint` | clean |
  | Web build (`tsc -b` + Vite) | passes |
  | Vitest | 57/57 |

  The skipped test is the redeploy scenario at `Quality/SessionLifetimeTests.cs:144`, deferred to roadmap step 4.
- The author of this record verified four findings directly against the code: N-01, N-02, N-08 and N-09.

## Prior findings (R-01 to R-29)

- **Resolved (25):** R-01, R-02, R-04–R-11, R-13–R-23, R-25, R-26, R-28 and R-29. Each has a test that would fail against the old code.
- **Partial (1):** R-03. The failure is now counted before the delay, on a token the client cannot cancel. But the delay itself can still be skipped by hanging up, so R-03 is folded into N-01.
- **Still deferred in spec §8 with owner and due date (3):** R-12, R-24 and R-27. R-12 is no worse than recorded.

## Trace summary

- Every §4 user story has at least one AC and a sad §6 flow. US-07 is an identifier property and has no separate flow.
- Every §5 AC is claimed by a task and reaches code and a test. Every UI AC reaches a component or shell test in `src/Uniqua.Projector.Web`.
- AC-09 stays deferred to roadmap step 8; its port-level part is complete and tested.
- The display-name-through-UI half of AC-01 and AC-04 stays deferred (R-12).
- The two ADR amendments the first review asked for are recorded:
  - the 5-minute delay ceiling, in spec §6 and ADR 0010;
  - the 1-hour activity-stamp slack, allowed by spec §6. N-04 covers the artifacts that still describe the old boundary.
- **Open gap:** AC-12 against a client that uses a timeout (N-01).

## Findings

All findings below were resolved with the owner as **Fix now**. They go back to `/sdd:implement` as follow-up tasks.

### Stage 1

| # | Finding | Cite | AC | Verdict |
|---|---|---|---|---|
| N-01 | The delay can be skipped by hanging up. A wrong password waits and a correct one answers at once, so a client that gives up after about the verification time (~100 ms) learns the answer without waiting. Parallel attempts each wait on their own, so throughput grows with how many run at once. The growing count only lengthens waits the attacker never sits through, so "guessing becomes futile" is not met. | `Application/Accounts/SignIn.cs:48-64` | AC-12 | Fix now: **a cap on failed sign-ins per request source**, counted before the delay and keyed by the trusted-proxy-resolved source that registration already uses. It refuses with a declared problem once reached. The owner, signing in from another source, is unaffected, and AC-12's wording stands. Record it as an ADR 0010 amendment. Record the remaining risk (one account attacked from many sources) in spec §6.1. |
| N-02 | A password over 128 characters is told "A password must be at least 8 characters long." R-21 removed `maxLength`, so this case is now reachable. The comment points to a note in `AccountProblems.cs` that no longer exists. | `Domain/Accounts/AccountErrors.cs:29-35`, `openapi.yaml:95,109`, `Web/.../RegisterScreen.tsx:125-127` | AC-01, AC-02 | Fix now. The detail names both bounds. Update openapi and the `AccountProblemsTests` row to match, and add a test with a 129-character password. |
| N-03 | The limits of the unregistered-address counter are written only in a code comment: a restart clears it, and past 100,000 entries new addresses go uncounted. The `IUnknownAddressAttempts` port is missing from sad §5. | `Infrastructure/Accounts/InMemoryUnknownAddressAttempts.cs:15-21`, `spec.md:219`, `sad.md:540` | AC-05b | Fix now (docs). Record the accepted residual risk in spec §6.1 and ADR 0010, and add the port to sad §5. |
| N-04 | test-plan and data-model still put the AC-07 boundary at 14 days ± 1 minute. The code now expires a session 14 days + 1 hour after its stamp. No decision record was written, although sad flagged the need for one. | `test-plan.md:50,77`, `data-model.md:268`, `sad.md:387,496`; code `Domain/Accounts/Session.cs:35,75-76` | AC-07 | Fix now (docs). Update test-plan and the data-model fixture, and add one line in sad flow 5 or an ADR 0008 amendment. |
| N-05 | The test-plan edge row "store unavailable → fail closed, sign-in form presented" has no test. The UI deliberately shows the failure view with a retry, not the sign-in form. | `test-plan.md:80`, `Api/.../SessionAuthenticationHandler.cs:55`, `Web/.../useSession.ts:91-93`, `AccountShell.tsx:38-50` | AC-10 | Fix now. Add an integration test in which a throwing session reader never yields a 200 from `/me`, and re-word the row to match the UI. |
| N-06 | Contract fidelity: (a) `AccountProblemsTests` has no `display_name_invalid` row and a stale comment saying the code is undeclared; (b) the openapi 429 example ends "Try again in 37 seconds.", which the server never sends, and the `StartsWith` check hides the mismatch; (c) a missing or malformed JSON body fails in model binding with a problem that has no `code`, and `/sessions` declares no 400. The test only asserts `>= 400`. | `tests/.../AccountProblemsTests.cs:18-43,67-68`, `openapi.yaml:168,225-256`, `Api/Accounts/AccountEndpoints.cs:20,83`, `RegisterEndpointTests.cs:161` | contract | Fix now. Add the row and remove the comment, make the 429 example match, route body-binding failures to a declared code and status, and tighten the test. |
| N-07 | Spec §8 still lists the ADR 0003 lockout divergence as open, although the Accepted ADR 0010 settles it. ADR 0003 does not point to ADR 0010. | `spec.md:239`, `docs/adr/0003-authenticate-with-identity-and-an-httponly-cookie.md:51` | AC-12 | Fix now (docs). Tick the item with a link to ADR 0010, and add a "see ADR 0010" note to ADR 0003. |

### Stage 2

| # | Finding | Cite | Verdict |
|---|---|---|---|
| N-08 | "Never succeeded" counts as stale, so the 48-hour alert fires on the first failed sweep after any start, for example when the database is not ready yet after a redeploy. The test covers only a success followed by a failure 49 hours later. | `Api/Accounts/ExpiredSessionCleanupService.cs:53-54,103,112-121` | Fix now. Measure staleness from `LastSucceededAt ?? processStartedAt`, and add a test in which a first run fails at t=0 and raises no alert. |
| N-09 | After 8 lost compare-and-set rounds, the blind increment is written but the method returns the stale `next`. The delay is then understated exactly when many guesses run in parallel. | `Infrastructure/Accounts/IdentityAccountStore.cs:199-210` | Fix now. Return the value actually written. |
| N-10 | A wrong password for a registered address costs FindByEmail, a by-id read, a SELECT and an UPDATE. An unregistered address costs FindByEmail plus in-memory work. The extra round trips show on attempts 1–5, before any delay masks them. | `Infrastructure/Accounts/IdentityAccountStore.cs:127-129,165-169`, `Application/Accounts/SignIn.cs:41-56` | Fix now. Seed the compare-and-set with the count already read (shared change with N-09). |
| N-11 | No test runs registrations in parallel against the per-source limit, or checks that `Release` of a refused decision is a no-op. No test covers the new throw on an unexpected Identity error (R-14). | `Api/Accounts/RegistrationRateLimit.cs:168-200`, `RegisterEndpointTests.cs:368-372`, `IdentityAccountStore.cs:104-119` | Fix now |
| N-12 | Switching between sign-in and registration unmounts the focused toggle, so keyboard focus falls to `<body>`. | `Web/src/features/auth/VisitorScreens.tsx:19-32` | Fix now. Move focus to the new form, and assert `toHaveFocus()`. |
| N-13 | The tokens T32 added put the input border at about 1.26:1 and the focus ring at about 2.6:1 against white, below WCAG 1.4.11's 3:1. | `Web/src/index.css:19-20`, `Web/src/components/ui/input.tsx:12-14` | Fix now |
| N-14 | Housekeeping: a crash dump is tracked at the repo root (added in d6d3fb7), and all 34 `tasks/*.md` files say `status: "todo"` while tracker.md marks them done. | `grep.exe.stackdump`, `docs/features/accounts-and-sessions/tasks/*.md` | Fix now |

## Gate

**CHANGES REQUESTED.**

- Seven stage-1 findings are Fix now (N-01–N-07). N-01 leaves AC-12 unmet against a client that uses a timeout, and it is the only one that is a missing behaviour. N-02–N-07 are wording, contract and document drift.
- Seven stage-2 findings are Fix now (N-08–N-14).
- Nothing is newly deferred. R-12, R-24 and R-27 stay deferred in spec §8.

After the fixes, re-review the changed surface:

- `SignIn`, the new per-source sign-in cap and its ADR 0010 amendment;
- `IdentityAccountStore.RecordFailureAsync`;
- `ExpiredSessionCleanupService`;
- the wording table and openapi;
- the body-binding problem path;
- `VisitorScreens` and `index.css`.
