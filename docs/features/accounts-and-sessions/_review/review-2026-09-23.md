# Review — accounts-and-sessions — 2026-09-23 (second re-review)

**Gate result: CHANGES REQUESTED**

## Scope

- This is the second re-review. The re-review of 2026-09-22 (`review-2026-09-22-2.md`, committed at 7abd4b5) asked for changes. Its fix-now findings were built as T35–T45 in the range `7abd4b5..HEAD` (HEAD 5b49a60). That range changes 84 files: 1,890 lines inserted and 238 deleted. Of those, 30 files are code or tests.
- The end-to-end trace was rebuilt over the whole feature, `0f05e30..HEAD`. It covers both declared surfaces, `backend-service` and `web-frontend`.
- The baseline was:
  - spec §4 and §5: 7 user stories and 18 ACs;
  - sad §6, data-model.md and contracts/openapi.yaml;
  - ADRs 0006–0011 and test-plan.md;
  - tasks.json, T1–T45.
- Three independent, clean-context `sdd:reviewer` passes ran:
  1. a finding-by-finding check of N-01 to N-14 against the current code and tests;
  2. a stage 1 end-to-end trace of every user story and AC, plus contract fidelity;
  3. a stage 2 quality pass over the fix range, backend and web.
- The gate was run separately:

  | Check | Result |
  |---|---|
  | `dotnet build` | 0 warnings, 0 errors |
  | Domain tests | 63/63 |
  | Integration tests (SQL Server container) | 248 passed, 1 skipped, 2 failed (timing flakes, below) |
  | `dotnet format --verify-no-changes` | exit 0 |
  | `oxlint` | clean |
  | Web build (`tsc -b` + Vite) | passes |
  | Vitest | 61/61 |

- The two failures are timing flakes, not regressions:
  - `Sustained_contention_returns_the_count_actually_written_not_a_stale_one` passes when run alone. Q-13 explains the race behind it.
  - `Sign_ins_sustain_the_throughput_row_as_a_regression_check_only` measures 2.4–3.3 sign-ins/s against a floor of 3.3, and passes about one run in three. The pre-fix commit 7abd4b5 behaves the same (3 runs: pass, fail at 3.3/s, pass), so it is not a regression.
- The skipped test is the redeploy scenario, still deferred to roadmap step 4.
- The author of this record verified P-01 directly against the code (`AccountEndpoints.cs:95-104` reserves the slot before `signIn.ExecuteAsync`) and ran the before-and-after throughput comparison.

## Prior findings (N-01 to N-14)

- **Resolved (12):** N-01–N-04, N-06–N-09 and N-11–N-14.
- **Resolved, with caveats:**
  - N-05: the new test is a guard against regression, not a proof. It only rejects 2xx, so a 401 would pass (Q-12).
  - N-11: the release-a-refused-decision test cannot fail (Q-12).
  - N-14: the effort total in tracker.md is stale (Q-15).
- **Partial (1):** N-10. The compare-and-set is now seeded, but a wrong password for a registered address still costs 2 more round trips than an unregistered one (FindByEmail, the by-id read in `VerifyPasswordAsync`, UPDATE), against FindByEmail alone. The owner decided to accept and record it (Q-10).
- Measured contrast for N-13: input border 3.64:1, focus ring 4.85:1.
- Still deferred in spec §8 with owner and due date: R-12, R-24, R-27, and AC-09 (to roadmap step 8).

## Trace summary

- All seven user stories have at least one AC and a sad §6 flow.
- Every §5 AC is claimed by a task and reaches code and a test on both surfaces.
- All 11 `accounts.*` codes match across openapi.yaml, `AccountProblems.cs` and `AccountProblemsTests.cs`.
- The ADR 0008 idle boundary (14 days + 1 hour) matches spec, sad and data-model.
- **Open gap:** the per-source sign-in cap added for N-01 contradicts AC-12 (P-01). It is also missing from sad §6, test-plan, `docs/session-rules.md` and the README (P-03).

## Findings

The owner resolved every finding below as **Fix now**, except Q-10, which is accepted and recorded. They go back to `/sdd:implement` as follow-up tasks.

### Stage 1

| # | Finding | Cite | AC | Verdict |
|---|---|---|---|---|
| P-01 | The cap refuses a correct password. `Reserve` runs before verification, so once a source holds 20 failures, a correct password from that source gets 429 and is never checked. A guesser behind the owner's shared address blocks the owner for up to 15 minutes. That address can be an office NAT or CGNAT, a proxy missing from `KnownProxies`, or the single `"unknown"` key when `RemoteIpAddress` is null. This contradicts "a correct password is still accepted immediately … without the account ever becoming unusable to its owner". The ADR 0010 amendment's "never held against the cap" is true of counting, not of refusing. No test pins it: the owner test switches source. | `Api/Accounts/AccountEndpoints.cs:95-104,197-211`; `spec.md:186,219`; `adr/0010…md:54`; `SessionEndpointTests.cs:226` | AC-12 | **Fix now: re-key the cap.** Count failures per (source, normalised email address), with a looser ceiling per source only, so a shared source blocks only the address under attack. Do not apply the cap when the source resolves to `"unknown"`. Test that a correct password for another address from a capped source gets 201. **Residual, owner accepted:** a guesser who shares the owner's source and targets the owner's own address, or a source that reaches the per-source ceiling, can still block for ≤15 min. Record it in spec §6.1 and the ADR 0010 amendment. Clarify AC-12's "never unusable" as "never by anyone who does not share the owner's source". Pin it with a test. |
| P-02 | The source key is the full IPv6 address, so a client holding a /64 gets a fresh budget for each address. That bypasses both per-source limits and brings back N-01 without bound. It also grows `_entries` without limit within the window, because only lazy pruning bounds it. IPv4-mapped and plain IPv4 forms of one client get separate keys. | `Api/Accounts/AccountEndpoints.cs:199-201`; `Api/Accounts/SlidingWindowLimiter.cs:45,96-119` | AC-12, AC-01b | Fix now. Apply `MapToIPv4()` to IPv4-mapped addresses, key IPv6 by its /64 prefix for both limits, and add a hard ceiling on tracked sources. Add tests. |
| P-03 | The docs do not trace the sign-in cap and carry stale wording. (1) sad §6 flow 4 has no source-cap `alt` branch, and flow 6 still shows hold-then-record, while the code records first. (2) `docs/session-rules.md` §4 and the README leave out the cap and still say "no lockout, ever". (3) test-plan.md has no rows for the cap (the 21st attempt is refused without verification; parallel attempts; unregistered address; another source) or for the sign-in 429 screen state. (4) "Registration only" wording remains in the places cited, including the log value `consequence=registration_limit_shared_by_all_callers`, and openapi says "more than 20 failed", although in-flight attempts also hold slots. | `sad.md:311-359,402-442,508,513-516,612,627`; `docs/session-rules.md:100-131`; `README.md:17-19`; `test-plan.md:63-80`; `spec.md:220`; `AccountProblems.cs:31,79`; `ProblemDetailsSetup.cs:54`; `AccountEndpoints.cs:188,206-208`; `openapi.yaml:282` | AC-12, AC-05b | Fix now, as one docs-and-contract task. Also extend `SessionRulesDocumentTests` so it checks the cap figure. |
| P-04 | The test-plan boundary row says the exact 14 days + 1 hour and 90 days instants are "live". `IsExpired` uses `>=`, and the tests assert "expired" at both instants. | `test-plan.md:77`; `Domain/Accounts/Session.cs:75-76`; `SessionTests.cs:65,93` | AC-07, AC-07b | Fix now (docs). Reword it as "one minute before → live; at it → dead", for both instants. |
| P-05 | Error-contract gaps. (a) In Development, minimal APIs set `ThrowOnBadRequest=true`, so a malformed body becomes a 500 in `UnhandledExceptionHandler` and never reaches the 400 reshape. The tests run as `Testing`, so they don't see it. (b) The 500 is not declared on any operation, and its body has no `code`, although `Problem.required` includes it. (c) A wrong Content-Type returns an uncoded, undeclared 415. | `Api/ProblemDetailsSetup.cs:24-39,118-131`; `ApiFactory.cs:79`; `openapi.yaml:195-206,264-277,398`; `docs/session-rules.md:149-150` | contract, AC-10 | Fix now. Handle `BadHttpRequestException` in the one handler, or set `ThrowOnBadRequest=false`, and add a test that boots in Development. Declare a shared coded 500. Map 415 on these paths to `accounts.request_malformed`. Add tests. |

### Stage 2

| # | Finding | Cite | Verdict |
|---|---|---|---|
| Q-09 | T41 measures staleness from process start. On an instance restarted more often than every 48 hours, a sweep that always fails never raises `session_cleanup_stale`. | `Api/Accounts/ExpiredSessionCleanupService.cs:47,61-62` | Fix now. Before the first success, use a short grace period (for example one hour), and add a test: first run fails, then another failure after the grace period raises the alert. |
| Q-10 | N-10 remainder: a wrong password for a registered address costs 2 more round trips than an unregistered one, on attempts 1–5. | `Infrastructure/Accounts/IdentityAccountStore.cs:127-129,170-192` | **Accept and record.** Record it in spec §6.1 as accepted residual risk for AC-05b, with the measured cost. No code change. |
| Q-11 | Missing proofs of the cap's claims: nothing aborts a sign-in in flight, although registration's `catch { Release(); throw; }` beside it would silently reopen N-01 if copied. Deleting `limit.Release(...)` on success leaves every test green. No test moves the clock past the window. | `AccountEndpoints.cs:106-121` vs `:46-58`; `GuessingProtectionTests.cs:79-99` | Fix now. Add endpoint tests: an abandoned delay (`AbandonDelays`) still counts; more than 20 successes are never refused; the window reopens after `SignInRateLimit.Window`. |
| Q-12 | Tests that cannot fail: (a) the release-a-refused-decision test passes with the `!IsPermitted` guard deleted, because `ReservedAt` is `default`; (b) the store-outage test rejects only 2xx, so a 401 would pass. | `RegisterEndpointTests.cs:395-430`; `SlidingWindowLimiter.cs:414`; `SessionRecognitionTests.cs:292-293` | Fix now. Make (a) discriminating, and have (b) assert 500 and a `code` other than `accounts.session_not_recognised`. |
| Q-13 | Flaky fixtures: (a) the contention test depends on real timing, can fail with correct code (it did in this gate run), and never confirms that the fallback ran; (b) `TestClock._delays` is a `List` written from parallel requests, and the burst test ignores the non-429 responses; (c) `TestPeerAddress.Fresh()` has 16 random bits, and `TestClock.Reset()` returns to the same instant, so an exhausted peer stays exhausted for the rest of the run. | `IdentityAccountStoreTests.cs:221-289`; `Fixtures/TestClock.cs:14,36,51-56`; `GuessingProtectionTests.cs:96-98`; `Fixtures/TestPeerAddress.cs:26-32` | Fix now. (a) Force the fallback with a `DbCommandInterceptor` and assert `contended=true`. (b) Use a thread-safe collection, and assert the other 20 are exactly 401. (c) Hand out unique addresses from a monotonic counter. |
| Q-14 | The throughput test sits at its floor on this machine, 2.4–3.3/s against 3.3/s, and passes about one run in three, before and after the fix range. | `Quality/LatencyBudgetTests.cs:116-145` | Fix now. Re-baseline it: warm up, take the median of several runs, or widen the CI margin. The real figure stays with the smoke run. |
| Q-15 | The effort total at tracker.md:54 counts 20 tasks, and there are 45. | `docs/features/accounts-and-sessions/tasks/tracker.md:54` | Fix now |

## Gate

**CHANGES REQUESTED.** Five stage-1 findings are Fix now (P-01–P-05). P-01 means the new per-source cap does not yet meet AC-12 as written. Six stage-2 findings are Fix now (Q-09, Q-11–Q-15), and Q-10 is accepted and recorded. Once the fixes are in, re-review the changed surface: the limiter keying, `RequestSource`, `ProblemDetailsSetup`, the cleanup service, the test fixtures, and the docs named in P-03.
